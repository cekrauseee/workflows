interface:
  display_name: "Orchestrate"
  short_description: "Delegate execution and accept evidenced results"
  default_prompt: "Use $workflow-orchestrate to coordinate this work, delegating execution and verification while retaining decisions and result acceptance."
