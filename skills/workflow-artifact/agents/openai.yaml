interface:
  display_name: "HTML artifact"
  short_description: "Create portable HTML explanations and reports"
  default_prompt: "Use $workflow-artifact to create a self-contained HTML explanation."
